import React from 'react';

function Example1 (props) {
    //props.message = 'Hello ENSF381'
    return (
        <div>
            <h1>{props.message}</h1>
        </div>
    );
};

export default Example1;
